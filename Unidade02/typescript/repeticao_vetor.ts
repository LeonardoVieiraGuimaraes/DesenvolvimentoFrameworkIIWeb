let colors = ["red", "green", "blue"];

let indice = 0;

while (indice < colors.length) {
  console.log(colors[indice]);
  document.write("<p>indice: " + indice + " ==> " + colors[indice] + "</p>");
  indice++;
}
